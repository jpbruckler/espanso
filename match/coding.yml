matches:
  - trigger: ":re.date"
    replace: "\\d{4}-\\d{2}-\\d{2}"
  - trigger: ":ps-cbh"
    replace: |
          <#
            .SYNOPSIS
              $|$
            .DESCRIPTION

            .PARAMETER

            .INPUTS

            .OUTPUTS

            .EXAMPLE

            .NOTES
          #>